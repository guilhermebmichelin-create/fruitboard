version: 2
# Dependabot remains the weekly update mechanism. The GitHub-native CodeQL and
# dependency review drafts for issue #148 (.github/workflows/codeql.yml and
# .github/workflows/dependency-review.yml) complement these updates; they do
# not replace them and change nothing in this file.
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "08:00"
      timezone: America/Sao_Paulo
    open-pull-requests-limit: 5
    commit-message:
      prefix: deps
    # @types/node must track the pinned Node 24.20.0 runtime (DEVELOPMENT.md
    # toolchain policy); TypeScript 7 is postponed until typescript-eslint
    # supports it and the Phase 2 checkpoint has passed (owner decision,
    # 2026-09-07).
    ignore:
      - dependency-name: "@types/node"
        versions: [">=25"]
      - dependency-name: "typescript"
        versions: [">=7"]
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "08:00"
      timezone: America/Sao_Paulo
    open-pull-requests-limit: 5
    commit-message:
      prefix: deps
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "08:00"
      timezone: America/Sao_Paulo
    open-pull-requests-limit: 5
    commit-message:
      prefix: deps
