# Installed queued-state and restart verification — 2026-09-12 (#107)

Status: **Evidence only; no merge, no production scanning, no Phase 2
acceptance or promotion.** This is an additive record. The earlier installed
journey records and the preserved S5 disposition are unchanged.

This run closes the installed queued-state observation gap and provides a
clean mid-scan hard-kill/restart record. It also keeps two similar-looking S5
timelines separate: a coalesced trigger that has already finished the old run
and owns a successor, versus a genuinely running lease recovered in place.

## 1. Provenance and isolation

- `origin/main` was fetched and verified as
  `3ebac5f7a76c3425620ceba59e6078b32fb6cd85`, parent
  `b2fb62c36a057985ab0eba02458f037fcb96c215`, subject
  `docs(#41): final 2026-09-10 refresh against live b2fb62c with #106 merged
(#91)`.
- The reviewed build worktree was `%TEMP%/opencode/agent2-s5-restart-pin`,
  branch `test/107-s5-restart-pin`, commit
  `19585dae7bef9ffdec06b71e0627df1b3f7ceb2f`, parent exactly
  `3ebac5f7a76c3425620ceba59e6078b32fb6cd85`. Its diff from `origin/main`
  contains only the 108-line storage regression test and the preserved
  `run-20260910-s5-disposition.md`; no product behavior was changed for this
  verification.
- The exclusive window used
  `scripts/foundation-smoke-lock.ps1` (SHA-256
  `312F0794FF4C96094B590394AD4D4CF496C7E019BDD96714EE517421FD1DF3A1`),
  lock owner `df742ef205bb47eb847f3fb6be73561b`, purpose
  `installed-journey-107-queued-restart-pinned`, and run root
  `%TEMP%/fruitboard-phase2-queued-restart-df742ef205bb47eb847f3fb6be73561b`.
  The lock was held across archive, install, launch, termination, restart,
  evidence capture, and uninstall, then released owner-only. No other
  `fruitboard-desktop` process was running and normal
  `%LOCALAPPDATA%/com.fruitboard.desktop` data was not touched.
- Existing Foundation Smoke data was reversibly moved to
  `archived-pre-run-foundation-smoke` under the run root before the new
  install. Its database was 14,946,304 bytes, SHA-256
  `59E6E3321F5BAB3512AA1DB6EF06EC492BDFAC2C01F1423126748399BE8A76C2`.
- Pinned tools: Node `v24.20.0`, pnpm `11.25.0`, Rust `1.98.1`, Cargo
  `1.98.1`, uv `0.12.9`, Python `3.11.16` / SQLite `3.53.1`. Host build:
  Windows `10.0.26200`.
- The exact package was built in the clean worktree with:

  ```text
  node scripts/prepare-foundation-sidecar.mjs
  pnpm.cmd --filter @fruitboard/desktop exec tauri build --ci --no-sign
    --config src-tauri/tauri.package.conf.json
    --features packaging-smoke,scan-console --bundles nsis
  ```

  The unsigned NSIS installer was 2,936,461 bytes,
  SHA-256 `8CEF97A95E995CC5E50A9D7A57103872F2568FD85B9F1DA5B144652288591C91`.
  `Cargo.lock` SHA-256 was
  `CCCB9275FA4951BE4B03B6C485DF998C7C59D523A8A3735B477D36599284BC95` and
  `pnpm-lock.yaml` SHA-256 was
  `F7B4189DDB42A18ED5E3DC697BABF0FFD2339B962C424AB3C63E35F2CCD41DE5`.

## 2. Fixtures and method boundary

Both fixtures were generated by the unmodified
`scripts/generate-synthetic-tree.mjs` using synthetic marker bytes only.
The generator's canonical manifest hashes and the persisted manifest-file
hashes are recorded separately:

| Fixture              | Seed       | Generator manifest SHA-256                                         | `manifest.json` file SHA-256                                       | Selected roots                                                               |
| -------------------- | ---------- | ------------------------------------------------------------------ | ------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| Primary              | `20260922` | `759016c67e00ad70e80e82824285631a16f760cb958c3a76d8225bd3e2295e21` | `81F5048C450FF13345D8978A8990736E3EBA48C42F81F87D85C8D4B578DC42B3` | `fixture/roots/shard-0000-sunset-beat` and sibling `shard-0001-chorus-draft` |
| Cancellation/restart | `20260923` | `49bee21260de14db6fc83043cf6abf5a980f840a057f05bc9132d544d4b5cb3d` | `75EA93D5029D54148DC631C03C888BC963C8726A7992D13DF9C758A653D5A5D9` | `cancellation-fixture/roots`                                                 |

Each full baseline manifest reports 10,000 FLP-named files, four other files,
five hardlink groups, five alias locations, and `hardlinkSupport: created`.
The selected large `roots` subtree contains 8,000 FLP files and the selected
primary leaves contain ten committed files. The five aliases are under the
separate top-level `aliases/` directory, outside every root scanned here;
therefore this run makes no installed hardlink-alias claim.

The review-only driver is
[`queued-state-restart.mjs`](queued-state-restart.mjs), SHA-256
`BC1D3C342A4ADE84EFA473B851AC7A82E302A12A37E6B448844F062C8886BC1D`.
It uses WebView2 CDP only to call the installed Tauri bridge and read the DOM.
After the target reports `running` with a non-null run ID, it makes no further
target `scan_now` call and does not mutate the target fixture. It calls
`scan_now` only for two other roots, then terminates the exact target PID and
copies the durable database before relaunching.

UI snapshots and direct native commands are labelled separately in the JSONL
transcript. The database copies were opened read-only with the pinned Python
interpreter; both copies report SQLite `PRAGMA integrity_check = ok`.

## 3. S5 timeline disposition

The preserved S5 artifacts from the earlier record remain the coalesced-
trigger case. In `%TEMP%/fruitboard-fixed-20260909-20260909-154848`, the
old job `01a087a4-9cf5-76d1-847e-d658cc8be729` and run
`01a087a4-a116-7231-bc8f-24f2ac06b93f` were already terminally
`interrupted` with `follow_up_requested` before the hard kill; the transcript
records the terminal finish at `19:28:31.155Z` and the kill at
`19:28:32.906Z`. Its queued successor
`01a087a4-abb4-7e21-9478-9d09fa71d2d6` is the continuation. A different job ID
there is expected and is not a restart-recovery defect.

The new run separately demonstrates the genuinely running-lease case:

| Case                     | Durable evidence                                                                                                                                                                                                                                                                                           | Interpretation                                                                                                                                                                     |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Coalesced seed follow-up | Requested target seed job `01a09539-ec30-77e0-841f-de6cf4d549a6` became `interrupted` with `follow_up_requested=1`; its run `01a09539-ec37-7430-9daf-73da413a447a` has `error_code=follow_up_requested`. A fresh job `01a09539-eea4-7933-9d10-da3b2a002bd5` with a fresh retry chain completed 8,000 rows. | The first run had finished as invalidated by a coalesced trigger. The successor, not the old job, is the continuation. This is the class represented by the preserved S5 artifact. |
| Genuine hard kill        | Immediately after exact-PID kill, job `01a0953a-0daa-75b2-911d-df2cec1f4946` was still `running`, attempt `1/4`, retry chain `01a0953a-0daa-75b2-911d-df3b787cc602`, with run `01a0953a-0db6-7711-a518-eb18a4fa9a59` `running` and `follow_up_requested=0`.                                                | This is a live lease at restart, with no target coalesced `scan_now` invalidation.                                                                                                 |
| Restart recovery         | The same job and retry chain reached attempt `2/4`; the old run became `interrupted/error_code=restart`, and fresh run `01a0953a-17ac-7671-b9b5-35539a17f6fb` became `completed` with 8,000 published rows. `not_before_ms` moved from `1789210004906` to `1789210007421`, preserving the restart backoff. | Same job, same retry chain, fresh run, preserved attempt/backoff. No new job ID was required.                                                                                      |

The pre-restart copy was taken at `2026-09-12T10:46:45.906Z`, 54 ms after
the kill completed at `10:46:45.852Z`. It is
`hard-kill-before-restart/fruitboard.db`, 14,938,112 bytes, SHA-256
`CCA9D0807C6DC3E4EAC0F0531FA65E87D6B3D004C7AD791A17256736FA0AA530`.
It contains 1,536 open staged observations but leaves the already published
8,000 target rows unchanged. The post-restart copy is
`final-database/fruitboard.db`, the same size, SHA-256
`89424324D2B3998DF8DF2D0D385858896451C30E1558492E62CDCE9507091420`, with
the interrupted stage discarded, the new 8,000-row stage published, and zero
staged observations remaining.

The code agrees with these two paths. In
`crates/storage-sqlite/src/execution.rs`:

- `recover_interrupted_tx` (lines 484–590) fences a genuinely running run,
  discards staging, and requeues the same job with its retry chain and
  backoff when attempts remain;
- `enqueue_scan_tx` (lines 725–810) coalesces a trigger onto a running job by
  setting `follow_up_requested`;
- `finish_scan_run` (lines 1262–1385) turns that invalidated run into
  `interrupted` and enqueues a fresh successor; and
- `set_scan_root_enabled_at` / `remove_scan_root_at` (lines 1465–1505)
  cancel queued work before changing root configuration.

The new storage regression test in the reviewed `19585da` worktree,
`kill_after_follow_up_invalidation_keeps_successor_and_leaves_old_interrupted`,
replays the coalesced-trigger shape and explicitly asserts that restart does
not resurrect the old terminal job merely because its ID differs.

## 4. Installed queued-state and queued invalidation observations

The transcript is
`queued-state-restart.jsonl`, 40,935 bytes, SHA-256
`0C044A48750B4405B647A78599CE27C59DE7B1C5EF977F5F6BAC56A6F8CBD04F`.

- After the target entered `running`, native `scan_now` calls for `Queued
Disable` and `Queued Remove` returned `queued` with `runId: null`. The
  native status snapshot was `running`, `queued`, `queued` for the three
  roots. The UI snapshot independently rendered `data-scan-progress` as
  `running`, `queued`, `queued`, with `data-library-state="stale-results"`,
  no percentage, and the target Cancel action.
- Disabling `Queued Disable` returned a successful root mutation. Its queued
  job `01a0953a-0dd3-7e71-b52b-7429b6198e53` stayed the same job and became
  `cancelled`; the final DB records `attempt=0/4`,
  `cancellation_requested=1`, and `last_error_code=root_invalidated`.
  The immediate DOM snapshot still held the cached queued progress label; the
  native durable status was already cancelled, and the post-restart UI showed
  `Execution Cancelled` and `Enable this root in Preferences`. The record
  does not claim an instantaneous renderer refresh after the mutation.
- Removing `Queued Remove` cancelled its queued job
  `01a0953a-0de2-7971-8d1a-6d459864edbf`, detached its ten committed
  locations, removed the root/status entry, and made a subsequent `scan_now`
  return typed `not_found`. The removed root is absent after restart; the
  detached locations remain in the final database without stale publication.
- The app was uninstalled from the run-specific install directory with exit
  code 0. The install directory was absent afterward, no app process remained,
  and the lock file was absent after owner-only release. The run root, fixture,
  archive, transcript, and database copies remain under `%TEMP%` for review.

## 5. Tests and acceptance disposition

The clean `19585da` worktree independently passed:

- `cargo test -p fruitboard-storage --locked --lib` — **77 passed**;
- `cargo test -p fruitboard-desktop --features scan-console --locked` —
  **92 passed**; and
- the feature-enabled NSIS build and installed smoke described above.

Checklist addendum mapping:

| Checklist step               | Addendum result                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4. Scan now                  | Installed native queued, running, and terminal states are now directly recorded. Queued UI labels are observed before mutation; the status API and restart UI record terminal cancellation.                                                                                                                                                                                                                                                    |
| 6. Restart survival          | Clean hard-kill copy proves a live target lease before restart; same job/retry chain, attempt 2, fresh run, backoff, and 8,000 committed rows prove recovery.                                                                                                                                                                                                                                                                                  |
| 9. Watcher follow-up         | Reuse [#106's fixed installed validation](run-20260909-fixed-validation.md#53-scenario-3--burst-changes-appearing-in-committed-library-results): five burst markers converged through exactly one installed follow-up to 15 rows, then removal produced missing rows. That run tested `ded02ac`; #106 records its verified patch identity to merged `cccaa67`, so it remains separate provenance rather than being relabelled as this package. |
| 10. Cancel/unavailable/retry | This run adds queued disable/remove evidence. The existing #106 record remains the installed evidence for cancellation, unavailable recovery, exhausted retry, and fresh-chain behavior; no claim is copied from its fake-adapter or deterministic suites.                                                                                                                                                                                     |

Remaining checklist gaps are intentionally unchanged:

- **Denied/ACL:** no installed denied-root or mid-watch ACL revocation was
  run. The deterministic fault-injection and portable/fake evidence remain
  separate; DriveFS, network shares, and ACL revocation are not qualified.
- **Resource limit:** this installed run selected 8,000 rows and did not
  exercise `ResourceLimit`. The accepted full baseline is 10,005 observations
  (10,000 FLP files plus five aliases) against the 10,000 staging quota, so F1
  remains an owner decision. F2 warm-p95 and F3 100,000-entry measurement
  remain open as recorded in the benchmark evidence.
- **Hardlinks/identity:** NTFS fixture generation reported
  `hardlinkSupport: created`, but all five aliases were outside the scanned
  roots. No installed per-path alias result is claimed; the deterministic
  storage/enumeration cases and the non-NTFS limits in #48 remain separate.

P2-03 through P2-08 remain unaccepted pending owner review of the integrated
evidence. This record does not change product behavior, merge the reviewed
commit, or promote Phase 2 acceptance.

## 6. Preserved diagnostic runs

Earlier harness attempts are retained and are not used as passing evidence:

- `fruitboard-phase2-queued-restart-9a75e7378d4447eea97db9438d22c386`
  stopped before lock acquisition because of process execution policy;
- `...-b9bce3f2932b424ab1ef0a4f6d0a36e4` exposed the seeding successor that
  motivated the helper correction;
- `...-110c6bf6cf494ec49e3a355c4ff9b769` independently completed the scenario
  with the system Node runtime; and
- `...-fd7e24c9aba84e26b06822d7eaa6c4dc` reached the same hard-kill point but
  stopped on WebView2's transient `about:blank` target before the title-gated
  discovery correction.

They remain outside the repository and were not deleted or folded into the
canonical pinned run above.
