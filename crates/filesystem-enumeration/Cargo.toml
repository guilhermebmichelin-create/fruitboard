[package]
name = "fruitboard-filesystem-enumeration"
version = "0.1.0"
edition = "2024"
rust-version = "1.98.1"
publish = false

# This crate is intentionally standalone while the shared Cargo workspace and
# storage/publication contracts are owned by other agents. Integration adds it
# to the root workspace after review; no shared manifest or lockfile is changed
# by this slice.
[workspace]
